$kind: http-request
url: "{{baseUrl}}/api/user/:id"
method: DELETE
pathVariables:
  - key: id
    value: "{{p2UserId}}"
    description: "(Required) "
auth:
  type: bearer
  credentials:
    token: "{{adminToken}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Удаление второго игрока → 2xx", function () {
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
order: 9000
