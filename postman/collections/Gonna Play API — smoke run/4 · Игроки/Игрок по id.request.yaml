$kind: http-request
url: "{{baseUrl}}/api/player/:id"
method: GET
pathVariables:
  - key: id
    value: "{{p1PlayerId}}"
    description: "(Required) "
auth:
  type: bearer
  credentials:
    token: "{{adminToken}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Игрок по id → 2xx", function () {
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
order: 3000
