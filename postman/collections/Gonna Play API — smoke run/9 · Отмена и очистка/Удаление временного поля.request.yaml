$kind: http-request
url: "{{baseUrl}}/api/field/:id"
method: DELETE
pathVariables:
  - key: id
    value: "{{fieldId2}}"
    description: "(Required) "
auth:
  type: bearer
  credentials:
    token: "{{adminToken}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Удаление временного поля → 2xx", function () {
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
order: 5000
