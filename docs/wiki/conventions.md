# Соглашения кода

Выжимка правил, как писать код в стиле проекта. Полный источник с ссылками `path:line` — `.claude/skills/backend-design/references/conventions.md`; при расхождении верь тому файлу и `package.json`.

## ESM

Проект собран как ESM (`"type": "module"`). Два правила, которые легко нарушить:

- Относительные импорты пишутся **с расширением `.js`**, даже для файлов `.ts`: `import PlayerRepository from "./player.repository.js"`.
- Импорты от корня `src/` — через алиас `@/`: `import prisma from "@/config/prisma.js"` (настроен в `tsconfig.json`).

## Модуль на ресурс

Каждый ресурс — папка `src/<resource>/` со слоями. Эталон — `src/player/`, но с оговоркой ниже.

- `<resource>.routes.ts` — регистрирует маршруты, привязывает схемы TypeBox через `{ schema: ... }`.
- `<resource>.controller.ts` — тонкий слой: собирает сервис в конструкторе, перекладывает `req.body` / `req.params` в вызовы сервиса.
- `<resource>.service.ts` — бизнес-логика; здесь объявляются `CreateX`, `UpdateX`, `IXRepository`.
- `<resource>.repository.ts` — доступ к данным только через Prisma, реализует `IXRepository`.
- `<resource>.model.ts` — схемы TypeBox (`createXSchema`, `updateXSchema`) и доменный класс.

`src/player/player.repository.ts` целиком на Prisma и годится как образец репозитория (`Map` в памяти давно убран). Любой новый репозиторий пишется так же — доступ к данным только через `this.prisma`.

**Слои упорядочены, и порядок теперь проверяет линт.** Поток идёт `routes → controller → service → repository`, модель стоит сбоку и не знает ни об одном из исполняющих слоёв. Обратные рёбра запрещены правилом `noRestrictedImports` в `biome.json` (см. «Автоматические ограничители»): ни сервис, ни репозиторий не импортируют `*.controller.ts` и `*.routes.ts`, а модель — ещё и сервис с репозиторием. Единственное ребро «снизу вверх», которое остаётся законным, — импорт репозиторием интерфейса из своего сервиса (`IXRepository` объявляется там же, где бизнес-логика): так написаны восемь репозиториев, и ломать их незачем. Но писать это ребро следует **только** как `import type` — рантайм-импорт значения из своего сервиса замыкает цикл модулей и ловится правилом `noImportCycles` (сейчас `warn`). Ровно два таких цикла в `src/match/` известны и пока приняты.

## Регистрация маршрутов

Все модули маршрутов подключаются в `src/router.ts` через `server.register(<resource>Routes, { prefix: "api/<resource>" })`. Новый ресурс — одна строка регистрации там же. С версионированием (Р5) префикс включает `/v1`.

## Обработка ошибок

Наружу любой ответ-ошибка — это конверт `{ code, message }` (тип `ErrorResponse`, `src/types/prisma.ts`), где `code` равен HTTP-статусу. Все коды — в каталоге `src/constants/errorCodes.ts` (реэкспорт через `index.ts`): доменные записи плюс секция `// --- Infrastructure ---` с `INTERNAL_SERVER_ERROR` (500), `ROUTE_NOT_FOUND` (404), `RATE_LIMIT_EXCEEDED` (429). Своих строковых литералов конверта в коде не заводим — только ссылки на каталог.

Есть два пути, оба сводятся к одному конверту:

- **Return-as-value (доменный путь).** Сервис **возвращает** запись из каталога, а не бросает; тип метода — `Promise<Entity | ErrorResponse>`. Контроллер тонко пробрасывает результат, а хук `preSerialization` в `buildApp` (`src/app.ts`) распознаёт конверт guard-ом `isErrorShape` (`src/utils/cache.ts`) и промотирует `code` в HTTP-статус. Бросать встроенные ошибки Fastify (`errorCodes.FST_ERR_*` из `import { errorCodes } from "fastify"`) — нельзя.
- **Thrown-путь.** Всё брошенное (валидация TypeBox, неизвестный роут, инфраструктурный сбой) перехватывает центральный `src/plugins/errorHandler.ts`, зарегистрированный в `buildApp` через `setErrorHandler`/`setNotFoundHandler`. Он нормализует бросок к тому же конверту: готовый конверт пропускает как есть, клиентскую ошибку (`statusCode < 500`) отдаёт как `{ code: statusCode, message }`, а 5xx и неизвестное логирует и заменяет генериком `INTERNAL_SERVER_ERROR` — внутренности наружу не текут.

Инфраструктурный сбой сервис логирует инъектированным `FastifyBaseLogger` и re-throw-ит — его подхватывает центральный обработчик.

## Prisma: имена

- Модель — PascalCase в единственном числе (`User`, `Match`).
- Таблица — через `@@map("<snake_case_plural>")` (`@@map("matches")`).
- Перечисления — SCREAMING_SNAKE_CASE, значения с `@map("lowercase")` (образец `PLAYER_POSITION`).
- Служебные поля у каждой модели: `id`, `createdAt`, `updatedAt`.
- Связи — явный `@relation`, `onDelete` осознанно (`Cascade` для зависимых, `SetNull` для необязательных).

## Клиенты инфраструктуры

Клиент Prisma и клиент Redis берутся **функциями в момент использования**, а не импортируются готовыми объектами: `getPrisma()` из `@/config/prisma.js` и `getRedis()` из `@/config/redis.js`. Обе создают клиент лениво при первом вызове и дальше отдают один и тот же объект, поэтому на всё приложение приходится ровно один пул Prisma и одно соединение Redis. Закрываются они парными `closePrisma()` / `closeRedis()` в `onClose` приложения.

Создавать `new PrismaClient()` или `new Redis()` в доменном коде — репозиториях, сервисах, контроллерах, плагинах — **нельзя**. Замена всегда одна: вызвать `getPrisma()` или `getRedis()` там, где клиент понадобился. Причина не стилистическая: собственный клиент открывает второй пул соединений к PostgreSQL или второе соединение к Redis, а созданный на импорте модуля — соединяется ещё и до того, как приложение решило подниматься, из-за чего юнит-тесты, которым база не нужна вовсе, начинают её требовать. Ровно этот дубль и был устранён — раньше приложение держало два пула Prisma, доменный и плагинный; единственность проверяется интеграционным тестом INT-25 (`app.prisma === getPrisma()`).

Ту же роль в тестах играет шов `setRedis()`: юнит-прогон подставляет им заглушку вместо живого соединения. Это единственный законный способ подменить клиент — прямая подмена модуля резолвером не нужна.

## Миграции

Миграции — в `prisma/migrations/`, команды обёрнуты в скрипты `package.json` (`prisma:migrate`, `prisma:migrate:create`, `prisma:generate`, `prisma:format`). Файл миграции готовится, но к живой базе **не применяется** автоматически — запуск `prisma migrate` на реальной БД остаётся за человеком.

## Тесты

Тесты пишутся **по коду**. Вика приведена к реализации 2026-07-23 (площадки `Location + Field`, refresh в Redis — см. [decisions.md](decisions.md)), так что теперь ей можно доверять по этим темам. Единственное намеренное расхождение — модуль `rating` / таблица `player_ratings`: код их содержит, но решение Р1 их отклоняет и они помечены к удалению, поэтому опирать на них тесты не нужно.

**Раскладка.** Всё живёт в `tests/`: `tests/unit/` — юнит-проект, `tests/integration/` — интеграционный, `tests/helpers/` — общие помощники (`createTestApp`, `createActor`, `relogin`, `resetDatabase`), `tests/setup/` — файлы настройки проектов. Два проекта описаны в `vitest.config.ts` и запускаются раздельно: `npm run test:unit` и `npm run test:integration`.

**Граница слоёв.** Юнит-тест проверяет сервис с репозиториями, подменёнными через конструктор, и с заглушкой Redis; инфраструктура не поднимается, клиент Prisma не создаётся вовсе. Интеграционный тест ходит по HTTP через `app.inject()` поверх реальной базы, которая чистится `resetDatabase()` перед каждым тестом. Отсюда следствия, на которых легко споткнуться:

- Актёров тест создаёт себе сам через `createActor`. Из сида не берётся никто: очистка сносит пользователей, и запись, на которую тест рассчитывал, к моменту прогона уже не существует.
- `playerId` и роли зашиты в JWT на момент выдачи. После создания игрока или изменения ролей нужен повторный логин через `relogin` — старый токен продолжает нести прежнее содержимое и молча ведёт тест не туда.
- Доменные ошибки сервисы **возвращают**, а не бросают. Юнит-тест ассертит возвращённое значение, интеграционный — HTTP-статус; `expect(...).rejects` здесь не сработает.
- Заглушка Redis не бросает исключение на неучтённый метод, а записывает его в список и роняет тест в `afterEach`. Бросок бесполезен: весь кеш-слой обёрнут в `try` с пустым `catch`, исключение ушло бы туда и тест зазеленел бы вхолостую.
- Тестовая база общая между параллельными сессиями, поэтому абсолютные утверждения о её состоянии за пределами того, что тест сам создал после `resetDatabase()`, ненадёжны.

**Плагины Fastify оборачиваются в `fastify-plugin`.** Без обёртки декоратор оседает в изолированном контексте плагина и на корневом экземпляре не виден. Это не теория: тест INT-25 нашёл ровно такой дефект в реальном коде.

**Одно приложение на файл.** Приложение строится в `beforeAll`, а не в `beforeEach`: сборка регистрирует пять плагинов и генерирует OpenAPI из схем роутов — это не бесплатно. Правило про скорость, а не про корректность: после развязки клиентов повторный `buildApp()` получает свежие клиенты, а не закрытые. Единственное сознательное исключение — INT-25, который именно повторную сборку и проверяет.

**Порог покрытия.** Живёт в `coverage.thresholds` (`vitest.config.ts`) и срабатывает только в `npm run test:coverage`, в том числе в контейнерном прогоне `docker-compose.test.yml`. Обычный `npm test` покрытие не считает — это осознанно: цикл разработки остаётся быстрым, а гейт держится в полном прогоне. Числа сняты с фактического прогона обоих проектов и округлены вниз до целого процента, чтобы дрожание метрики не роняло прогон на неизменившемся коде. Пересмотр — **только вручную**: понижение отдельным коммитом с объяснением, повышение при росте покрытия — тоже руками. `thresholds.autoUpdate` намеренно не используется: он переписывает конфигурацию во время прогона, из-за чего рабочее дерево меняется само собой, а в контейнерном прогоне это ещё и правка внутри bind-mount. Текущие числа заведомо занижены и целью не являются — матчевый домен тестами пока не покрыт, и после его появления порог подлежит пересмотру вверх.

## Автоматические ограничители

Часть соглашений выше проверяется машиной, а не глазами на ревью. Источники правды — `biome.json` (правила линтера и послойные `overrides`), `biome/plugins/no-nested-loops.grit` (запрет вложенных циклов) и `scripts/check-comments.mjs` (бюджет комментариев). Локально всё это запускается одной командой `npm run lint` (`biome ci .` плюс скрипт бюджета); отдельно — `npm run lint:comments`, `npm run lint:report` (метрики по каждому файлу, всегда код 0) и `npm run lint:fix`. Где именно гейт срабатывает помимо ручного запуска — в README.

### Пороги

| Ограничитель | Порог | Правило | Почему такой |
| --- | --- | --- | --- |
| Когнитивная сложность функции | 15 | `complexity/noExcessiveCognitiveComplexity` (error) | Действует давно, но нигде не был описан. Выше 15 функция перестаёт читаться целиком. |
| Длина функции | 80 непустых строк | `complexity/noExcessiveLinesPerFunction` (error, `skipBlankLines`) | Тоже действует давно. Функция длиннее экрана скрывает своё ветвление. |
| Размер файла | 200 непустых строк в `src/**` и по умолчанию, 300 в `tests/**`, 600 в `scripts/**` | `style/noExcessiveLinesPerFile` (error, `skipBlankLines`) | Медиана файла в `src/` — 58 строк, среднее 79, так что 200 — потолок, а не норма. Тестам разрешено больше: таблица случаев растёт линейно и дробить её вредно. Скриптам ещё больше: `scripts/postman-smoke.mjs` — один самодостаточный сценарий на 535 строк. `prisma/seeds/**` порог 600 **не** получает и остаётся на 200 — поэтому область скриптов вынесена отдельным блоком `overrides`, а не дописана в соседний блок, где `scripts/**` и `prisma/seeds/**` стоят вместе. |
| Серия комментариев подряд | 12 строк | `check-comments/comment-run` | Фактический максимум в `src/` — 8 строк. Объяснение длиннее абзаца переезжает в `docs/wiki/`, а в коде остаётся ссылка. |
| Доля строк-комментариев | 0.35 от непустых строк, для файлов от 40 непустых строк | `check-comments/comment-ratio` | Фактический максимум — 27.9%. Короткие файлы не проверяются: у них доля скачет от одной строки. JSDoc в числитель не входит — он документирует публичный контракт, наказывать за него нечего. |
| Закомментированный код | 0 вхождений | `check-comments/commented-out-code` | История кода живёт в git. Признаки, по которым строка считается кодом, перечислены в шапке `scripts/check-comments.mjs`; директивы инструментов, `TODO`, `FIXME`, `NOTE` под них не подпадают. |
| Вложенные циклы | запрещены | GritQL-плагин `biome/plugins/no-nested-loops.grit` (error, область `**/src/**/*.ts`) | Нативного правила нет: `noExcessiveNestedCallbacks` считает коллбэки и `for`/`while` не видит. Диагностика вешается на внешний цикл; поиск транзитивен (цикл внутри `switch` или `if` ловится), но останавливается на границе функции — цикл внутри коллбэка внутри цикла остаётся за `noExcessiveCognitiveComplexity`. |

Плюс правила без числа: `correctness/useImportExtensions` (error) кодирует расширение `.js` в относительных импортах, `nursery/useImportsFirst` (error) — импорты в начале файла, `suspicious/noImportCycles` (пока `warn`) — циклы модулей.

### Направления импорта

Правило `noRestrictedImports` не умеет спрашивать «из какого файла импортируют», поэтому привязка к слою сделана блоками `overrides[].includes` в `biome.json`. Запреты, действующие во всём `src/`:

- Импорт за пределы своей папки относительным путём (`../**`) — пишется через алиас `@/`.
- Импорт сгенерированного клиента (`@/generated/**`) — клиент берётся `getPrisma()`. Исключение и единственные легальные точки доступа — `src/config/prisma.ts` и `src/types/prisma.ts`.
- Рантайм-импорт `errorCodes` из `fastify` — доменные ошибки возвращаются записью каталога `src/constants/errorCodes.ts`.

Послойные запреты поверх этого:

| Файлы | Что запрещено импортировать |
| --- | --- |
| `src/*/*.model.ts` | `*.controller`, `*.routes`, `*.service`, `*.repository` — своего домена и чужих |
| `src/*/*.repository.ts` | `*.controller`, `*.routes` |
| `src/*/*.service.ts` | `*.controller`, `*.routes` |
| `src/config/**`, `src/utils/**`, `src/types/**`, `src/constants/**` | любой домен (`@/auth/**`, `@/user/**`, `@/player/**`, `@/match/**`, `@/field/**`, `@/location/**`, `@/rating/**`, `@/role/**`) — зависимость направлена от домена к общему низу |

Кросс-доменные рёбра (сервис одного домена берёт репозиторий или сервис другого) остаются законными и не запрещаются ничем: на них держится, например, `src/auth/auth.service.ts`. Каждый послойный паттерн выписан в двух формах — относительной (`./*.service.js`) и алиасной (`@/*/*.service.js`), потому что в коде встречаются обе, а глоб сопоставляется со спецификатором ровно так, как тот написан.

### Именные послабления

Полный список того, где правила ослаблены, и почему. Любое послабление понижает степень до `warn` или выключает правило точечно — молча ослаблять порог для всех нельзя.

| Область | Послабление | Причина |
| --- | --- | --- |
| `tests/**` | `noMagicNumbers` off, `noExcessiveLinesPerFunction` off, размер файла 300 | Числа в тестах — это данные случая, а не магия; таблица случаев в одном `describe` длиннее любой продуктовой функции. |
| `scripts/**`, `prisma/seeds/**` | `noConsole` off | Вывод в консоль — единственный интерфейс скрипта. |
| `scripts/**` | размер файла 600 | См. выше про `postman-smoke.mjs`. |
| `src/config/env.ts`, `src/config/redis.ts`, `src/utils/rateLimit.ts`, `src/utils/cache.ts`, `src/types/pagination.ts`, `src/auth/auth.service.ts`, `src/auth/auth.routes.ts` | `noMagicNumbers` → `warn` | Таймауты, TTL и лимиты, вынос которых в константы не сделан. Долг, а не норма. |
| `src/match/match.service.ts` | `noExcessiveCognitiveComplexity`, `noExcessiveLinesPerFunction`, `noNonNullAssertion`, `noExcessiveLinesPerFile` → `warn` | 607 строк матчевой логики. Дробление файла — отдельная задача, а не побочный эффект введения линта. |
| `src/auth/auth.service.ts` | `noExcessiveLinesPerFile` → `warn` | Не укладывается в 200 даже после вычета пустых строк. |
| `src/player/player.service.ts` | `noRestrictedImports` → `warn` | Единственный оставшийся бросок `errorCodes` из `fastify`. Перевод пяти бросков на каталог меняет сигнатуры сервиса, ветвление контроллера и интеграционные тесты. |
| `tests/unit/check-comments.test.ts`, `tests/unit/guardrails.test.ts` | `noExcessiveLinesPerFile` → `warn` | Тесты самих ограничителей: каждая граница порога — отдельный случай, и их много. |

**Ослабление порога — отдельным коммитом с объяснением**, по той же политике, что и порог покрытия выше. Порог, тихо поднятый в коммите с фичей, перестаёт быть порогом. Повышение обратно — тоже руками и тоже отдельно.

### Две неочевидные детали конфига

Обе установлены прогонами, и на обеих легко ошибиться при правке `biome.json`.

**Счётчик строк Biome при `skipBlankLines` систематически ниже числа непустых строк файла.** Пороги `noExcessiveLinesPerFile` и `noExcessiveLinesPerFunction` нельзя калибровать по `wc -l` или по подсчёту непустых строк своими средствами: реальное число, с которым сравнивается порог, меньше, и насколько — заранее не сказать. Единственный надёжный способ узнать, укладывается ли файл, — прогнать по нему Biome и прочитать число из диагностики. Именно так выяснилось, что `src/auth/auth.service.ts` послабление нужно, а решать это «на глаз» по размеру файла — гадание.

**Понижение степени голой строкой наследует опции, а появление `options` наследование отменяет.** Запись `"noRestrictedImports": "warn"` в блоке `overrides` сохраняет весь набор паттернов, унаследованный от совпадающих блоков, — правило продолжает ловить те же импорты, только степенью ниже (так сделано послабление для `src/player/player.service.ts`). Но как только в блоке появляется `options`, набор перестаёт сливаться с внешним и замещает его целиком: всё, что не выписано в этом блоке, молча исчезает. Поэтому каждый послойный блок в `biome.json` повторяет базовый набор (`../**`, `@/generated/**`, `paths.fastify`) дословно, а не только свои послойные паттерны. Забыть одну строку — значит бесшумно снять защиту ровно на том слое, где она нужна: без дубля `paths.fastify` в блоке `src/*/*.service.ts` запрет `errorCodes` исчез бы именно на сервисах, где живёт единственный нарушитель.

## Коммиты

Формат — Conventional Commits: `<тип>(<область>): <описание>` (тип и описание на английском, в повелительном наклонении, без точки в конце). Типы: `feat`, `fix`, `chore`, `docs`, `refactor`, `test`. Область — модуль или срез (`auth`, `match`, `wiki`); допустимо опускать. Тело коммита — списком, по строке на смысловое изменение. Образец в истории: `chore(base): consolidate backend foundation and design wiki`.
