$kind: http-request
url: "{{baseUrl}}/api/role/assign"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: '{ "userId": {{orgUserId}}, "roleId": {{roleId}} }'
auth:
  type: bearer
  credentials:
    token: "{{adminToken}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Назначение роли организатору → 2xx", function () {
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
order: 3000
