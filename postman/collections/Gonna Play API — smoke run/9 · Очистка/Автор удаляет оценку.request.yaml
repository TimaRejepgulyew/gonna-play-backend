$kind: http-request
url: "{{baseUrl}}/api/rating/:id"
method: DELETE
pathVariables:
  - key: id
    value: "{{ratingId}}"
    description: "(Required) "
auth:
  type: bearer
  credentials:
    token: "{{orgToken}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Автор удаляет оценку → 2xx", function () {
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
order: 3000
