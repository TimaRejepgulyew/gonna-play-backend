$kind: http-example
request:
  url: "{{baseUrl}}/api/match/:id/participants?status=REGISTERED"
  method: GET
  queryParams:
    - key: status
      value: REGISTERED
      description: " (This can only be one of REGISTERED)"
  pathVariables:
    - key: id
      value: <string>
      description: "(Required) "
  body:
    type: text
    content: ""
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
