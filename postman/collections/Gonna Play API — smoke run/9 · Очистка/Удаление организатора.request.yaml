$kind: http-request
url: "{{baseUrl}}/api/user/:id"
method: DELETE
pathVariables:
  - key: id
    value: "{{orgUserId}}"
    description: "(Required) "
auth:
  type: bearer
  credentials:
    token: "{{adminToken}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Удаление организатора → 2xx", function () {
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
order: 8000
