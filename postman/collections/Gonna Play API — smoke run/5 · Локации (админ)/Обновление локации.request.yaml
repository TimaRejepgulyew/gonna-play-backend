$kind: http-request
url: "{{baseUrl}}/api/location/:id"
method: PUT
headers:
  Content-Type: application/json
pathVariables:
  - key: id
    value: "{{locationId}}"
    description: "(Required) "
body:
  type: json
  content: '{ "name": "Smoke Arena updated" }'
auth:
  type: bearer
  credentials:
    token: "{{adminToken}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Обновление локации → 2xx", function () {
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
order: 5000
