$kind: http-request
name: Организатор убирает свою запись участника
url: "{{baseUrl}}/api/match/:id/participants/:pid/decline"
method: POST
pathVariables:
  - key: id
    value: "{{matchId}}"
    description: "(Required) "
  - key: pid
    value: "{{orgPid}}"
    description: "(Required) "
auth:
  type: bearer
  credentials:
    token: "{{orgToken}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Организатор убирает свою запись участника → 2xx", function () {
        pm.expect(pm.response.code).to.be.within(200, 299);
      });
    language: text/javascript
order: 2000
